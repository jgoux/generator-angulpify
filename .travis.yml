language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angulpify' ]; then cd .. && eval "mv $currentfolder generator-angulpify" && cd generator-angulpify; fi

